// Runner for multiproxy tests
import {deploy_commit} from '../deploy';

var levelup = require('levelup');
// Start the test processes.
//var inprogress = deploy_commit(deployment_path, base_hostname, treeish);

//inprogress.on('start', function () {
    //ws.send("STARTED");
//});
//inprogress.on('end', _ => ws.close(1000, "ENDED"));
//inprogress.on('error', function(err){
    //console.error(err);
    //ws.close(1003, "ERROR " + err.toString());
//});
//inprogress.on('progress', function(progress) {
    //ws.send("PROGRESS " + progress);
//});
//started = true;

// Run all the test cases.

// pass test mode env variable

// pass snapshot in header? or env?

// record results.

//
